from setuptools import setup, find_packages

setup(
    name='Word_Frequency_Counter',
    version='1.0',
    packages=find_packages(),
    packages=['Word_Frequency_Counter'],
    install_requires=[
    ],
    # Add additional configuration as needed
)
